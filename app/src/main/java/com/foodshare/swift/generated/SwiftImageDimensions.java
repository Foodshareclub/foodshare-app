// Generated by jextract-swift
package com.foodshare.swift.generated;

import org.swift.swiftkit.core.annotations.*;

/**
 * Image dimensions result from Swift ImageProcessorEngine.
 */
@ThreadSafe
public final class SwiftImageDimensions implements AutoCloseable {

    static {
        System.loadLibrary("FoodshareCore");
    }

    private final long pointer;
    private boolean closed = false;

    SwiftImageDimensions(long pointer) {
        this.pointer = pointer;
    }

    public int getWidth() {
        checkNotClosed();
        return $getWidth(pointer);
    }

    public int getHeight() {
        checkNotClosed();
        return $getHeight(pointer);
    }

    private void checkNotClosed() {
        if (closed) {
            throw new IllegalStateException("SwiftImageDimensions already closed");
        }
    }

    @Override
    public void close() {
        if (!closed) {
            $destroy(pointer);
            closed = true;
        }
    }

    // Native method declarations
    private static native int $getWidth(long pointer);
    private static native int $getHeight(long pointer);
    private static native void $destroy(long pointer);
}
